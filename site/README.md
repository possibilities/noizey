# Noizey site

The public product, privacy, and support site for Noizey. It is a static,
anonymous-compatible vinext site deployed through Sites.

## Development

Requires Node.js 22.13 or newer.

```sh
npm install
npm run dev
```

Validate the production build and rendered page with:

```sh
npm test
```
